/**
 * The routes in the frontend.
 */
export enum Routes {
  Status = "/status",
  Login = "/login",
  Logout = "/logout",
  ProfileForm = "/profile",
  ConfirmationForm = "/confirm",
  Settings = "/settings",
  Statistics = "/statistics",
  Admission = "/admission",
  VerifyEmail = "/verify",
}
