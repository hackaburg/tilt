/**
 * The routes in the frontend.
 */
export enum Routes {
  Status = "/status",
  Login = "/login",
  Logout = "/logout",
  ProfileForm = "/profile",
  Settings = "/settings",
  Statistics = "/statistics",
  Admission = "/admission",
  VerifyEmail = "/verify",
}
