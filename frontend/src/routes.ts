/**
 * The routes in the frontend.
 */
export enum Routes {
  Activity = "/activity",
  Dashboard = "/dashboard",
  Login = "/login",
  Logout = "/logout",
  Settings = "/settings",
  Statistics = "/statistics",
  Users = "/users",
  VerifyEmail = "/verify",
}
