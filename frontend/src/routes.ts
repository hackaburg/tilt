/**
 * The routes in the frontend.
 */
export enum Routes {
  Dashboard = "/",
  Login = "/login",
  Logout = "/logout",
  Settings = "/settings",
  Activity = "/activity",
  Users = "/users",
  Statistics = "/statistics",
  Questions = "/questions",
}
