/**
 * The routes in the frontend.
 */
export enum Routes {
  Dashboard = "/dashboard",
  Login = "/login",
  Logout = "/logout",
  ProfileForm = "/profile",
  Settings = "/settings",
  Statistics = "/statistics",
  Users = "/users",
  VerifyEmail = "/verify",
}
