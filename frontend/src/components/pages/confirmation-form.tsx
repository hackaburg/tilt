import * as React from "react";
import { Form, FormType } from "../forms/form";

/**
 * The form to confirm a spot.
 */
export const ConfirmationForm = () => {
  return <Form type={FormType.ConfirmationForm} />;
};
