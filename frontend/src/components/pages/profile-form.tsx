import * as React from "react";
import { Form, FormType } from "../forms/form";

/**
 * The profile form, d'uuh.
 */
export const ProfileForm = () => {
  return <Form type={FormType.ProfileForm} />;
};
