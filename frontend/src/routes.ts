/**
 * The routes in the frontend.
 */
export enum Routes {
  Admission = "/admission",
  ConfirmationForm = "/confirm",
  Login = "/login",
  Logout = "/logout",
  ProfileForm = "/profile",
  Settings = "/settings",
  Statistics = "/statistics",
  Status = "/status",
  System = "/system",
  VerifyEmail = "/verify",
}
