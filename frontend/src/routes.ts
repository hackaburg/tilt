/**
 * The routes in the frontend.
 */
export enum Routes {
  Activity = "/activity",
  Dashboard = "/",
  Login = "/login",
  Logout = "/logout",
  Settings = "/settings",
  Statistics = "/statistics",
  Users = "/users",
}
